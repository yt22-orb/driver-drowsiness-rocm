# ROCm rig results

Date:

## Hardware and software

- GPU:
- VRAM:
- Ubuntu:
- Kernel:
- ROCm:
- PyTorch:
- Docker image digest:

## Commands and results

| Command | Result | Duration | Notes |
| --- | --- | ---: | --- |
| `make rocm-check` | Pending | | |
| `make audit` | Pending | | |
| `make train` | Pending | | |
| `make evaluate` | Pending | | |
| `make export` | Pending | | |
| `make web-build` | Pending | | |

## Final metrics

Paste `artifacts/evaluation/test-metrics.json` here or summarize it exactly.

## Failures and fixes

Record the full error, cause, changed files, and rerun result.

## Artifact hashes

```bash
sha256sum artifacts/runs/mobilenet-v3-small/best.pt \
  artifacts/export/drowsiness-mobilenet-v3-small.onnx \
  artifacts/export/model-metadata.json
```
